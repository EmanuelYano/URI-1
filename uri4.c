#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main()
{
	int a, b, PROD;
	scanf("%d %d",&a,&b);
	PROD = a * b;
	printf("PROD = %d\n", PROD);
	return 0;
}